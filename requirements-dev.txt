# Transkrybe.ai — development dependencies
# Testing, formatting, and notebook tooling. Install alongside
# backend/requirements.txt.

# Testing framework
pytest>=8.0.0
pytest-cov>=4.0.0
httpx>=0.27.0  # required by fastapi.testclient.TestClient (API router tests)

# Code quality and formatting
black>=24.0.0
flake8>=7.0.0
isort>=5.0.0
pre-commit>=3.0.0

# Optional — for exploratory notebook work against the engines. Not needed to
# run the application or the test suite; no notebooks ship with the release.
ipython>=8.0.0  # better interactive shell
ipykernel>=6.0.0
ipywidgets>=7.7.0
pandas>=2.0.0
